[project]
name = "internal"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyright>=1.1.410",
]

[tool.uv.workspace]
members = ["python/*"]

[dependency-groups]
dev = [
    "pyright>=1.1.410",
    "ruff>=0.15.19",
]
